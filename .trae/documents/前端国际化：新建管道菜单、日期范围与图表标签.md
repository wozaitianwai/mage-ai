**目标与结果**

* 已完成前端“新建管道”菜单、日期范围显示、以及仪表盘图表标签的国际化。

* 修复了用户反馈的剩余未翻译部分，特别是动态时间显示和下拉菜单项。

**代码改动**

* **新建管道菜单国际化**

  * 修改 `e:\Project\mage\mage_ai\frontend\components\Dashboard\utils.tsx`:

    * 更新 `getNewPipelineButtonMenuItems` 函数签名，增加 `t` 参数。

    * 将菜单项标签改为使用 `t('pipeline.type.*')` 和 `t('pipeline.new_menu.*')`。

  * 修改 `e:\Project\mage\mage_ai\frontend\pages\overview\index.tsx`:

    * 调用 `getNewPipelineButtonMenuItems` 时传入 `t` 函数。

* **日期范围显示国际化**

  * 修改 `e:\Project\mage\mage_ai\frontend\pages\overview\index.tsx`:

    * 在 `selectedDateRange` 的计算中，替换原有的 `getFullDateRangeString` 调用。

    * 改为使用 `moment(d).format('YYYY-MM-DD')` 进行本地格式化。这种格式（如 `2025-11-20 - 2025-12-19`）既清晰又语言中立，解决了中文环境下英文日期格式（November 20, ...）的问题。

* **仪表盘图表标签国际化**

  * 修改 `e:\Project\mage\mage_ai\frontend\components\charts\BarStack.tsx`:

    * `BarStackChart` 组件新增 `legendLabelFormat` 属性，并将其传递给 `@visx/legend` 的 `LegendOrdinal` 组件。

  * 修改 `e:\Project\mage\mage_ai\frontend\pages\overview\index.tsx`:

    * 在使用 `BarStackChart` 时传入 `legendLabelFormat={label => t('dashboard.' + label)}`，实现图例（Completed, Failed 等）的翻译。

**翻译字典更新**

* **英文 (`en/common.json`)**:

  * 新增 `pipeline.type` 和 `pipeline.new_menu` 下的键值。

  * 新增 `dashboard.cancelled/completed/failed/initial/running` 等状态词。

* **中文 (`zh/common.json`)**:

  * 对应新增中文翻译，如 "标准 (Standard/batch)"、"数据集成 (Data integration)"、"已完成"、"失败" 等。

**验证**

* 运行 `npm run lint` 通过，未引入新的 lint 错误（现有错误为既有遗留）。

* 代码逻辑检查确认：

  * 下拉菜单现在会根据语言切换显示中文或英文。

  * 日期范围将始终显示为 ISO 格式（YYYY-MM-DD），符合国际化通用习惯。

  * 图表下方的图例（Legend）将显示翻译后的状态文本。

**后续建议**

* 关注 `moment` 的本地化配置。虽然 `YYYY-MM-DD` 是通用的，但如果需要 "X月X日" 等格式，需确保 frontend 项目正确加载了 `moment` 的 locale 数据（通常在 `_app.tsx` 或入口处引入）。目前方案使用通用格式最为稳妥。

